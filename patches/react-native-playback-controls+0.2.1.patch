diff --git a/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/ControlsPlayer.kt b/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/ControlsPlayer.kt
index 276f2ea..c1b96fb 100644
--- a/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/ControlsPlayer.kt
+++ b/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/ControlsPlayer.kt
@@ -168,6 +168,8 @@ class ControlsPlayer : SimpleBasePlayer(Looper.getMainLooper()) {
       C.TIME_UNSET
     }
 
+    // Live radio has nothing to seek: advertise the item as non-seekable so
+    // the OS never renders a scrubber that couldn't do anything.
     return MediaItemData.Builder(MEDIA_ITEM_ID)
       .setMediaItem(
         MediaItem.Builder().setMediaId(MEDIA_ITEM_ID).setMediaMetadata(mediaMetadata).build()
@@ -175,7 +177,7 @@ class ControlsPlayer : SimpleBasePlayer(Looper.getMainLooper()) {
       .setMediaMetadata(mediaMetadata)
       .setDurationUs(durationUs)
       .setIsDynamic(metadata.isLiveStream)
-      .setIsSeekable(true)
+      .setIsSeekable(false)
       .build()
   }
 }
diff --git a/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/PlaybackControlsService.kt b/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/PlaybackControlsService.kt
index eaa0527..8a1361c 100644
--- a/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/PlaybackControlsService.kt
+++ b/node_modules/react-native-playback-controls/android/src/main/java/com/playbackcontrols/PlaybackControlsService.kt
@@ -1,5 +1,6 @@
 package com.playbackcontrols
 
+import android.app.PendingIntent
 import android.content.Intent
 import androidx.media3.session.MediaSession
 import androidx.media3.session.MediaSessionService
@@ -29,7 +30,23 @@ class PlaybackControlsService : MediaSessionService() {
     }
 
     val controlsPlayer = ControlsPlayer()
-    val session = MediaSession.Builder(this, controlsPlayer).build()
+    val sessionBuilder = MediaSession.Builder(this, controlsPlayer)
+
+    // Tap-to-open: media3's notification provider uses the session activity as the
+    // notification's content intent — without it, tapping the media card does nothing.
+    // Route the tap through the package's launch intent so the notification opens the app.
+    packageManager.getLaunchIntentForPackage(packageName)?.let { launchIntent ->
+      sessionBuilder.setSessionActivity(
+        PendingIntent.getActivity(
+          this,
+          0,
+          launchIntent,
+          PendingIntent.FLAG_IMMUTABLE,
+        )
+      )
+    }
+
+    val session = sessionBuilder.build()
 
     player = controlsPlayer
     mediaSession = session
@@ -45,7 +62,28 @@ class PlaybackControlsService : MediaSessionService() {
 
   override fun onTaskRemoved(rootIntent: Intent?) {
     super.onTaskRemoved(rootIntent)
+
+    // onTaskRemoved fires for ANY task belonging to this app — including
+    // auxiliary ones such as expo-web-browser's BrowserProxyActivity (SDK 55+
+    // runs the OAuth browser in a separate `.webContainer` task). Only the
+    // removal of the app's *own* task means the JS runtime is gone; acting on
+    // the others would tear down the media session (or kill the process) the
+    // moment the auth browser returns, closing the app mid-login.
+    val removedTask = rootIntent?.component
+    val appTask = packageManager.getLaunchIntentForPackage(packageName)?.component
+    if (removedTask == null || appTask == null || removedTask != appTask) {
+      return
+    }
+
     releaseSessionAndStop()
+    // The media session is only a *facade*: the real audio lives in
+    // expo-audio's ExoPlayer in this same process and is not reachable from
+    // here. Tearing the session down stops the notification but leaves the
+    // audio decoding, so swiping the app away produced sound with no app and
+    // no controls. Terminate the process so removal deterministically ends
+    // playback, matching the session's "controls never outlive the JS"
+    // contract in the class comment above.
+    android.os.Process.killProcess(android.os.Process.myPid())
   }
 
   override fun onDestroy() {
